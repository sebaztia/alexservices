server:
  port: 8081

spring:
  application:
    name: fraud
  datasource:
    username: 'alex'
    password: 'password'
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/alexdb
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true